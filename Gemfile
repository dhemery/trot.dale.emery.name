source 'https://rubygems.org'
common_gemfile = File.join('diddleman', 'Gemfile')
site_gemfile = File.join('site', 'Gemfile')

eval(IO.read(common_gemfile), binding)
eval(IO.read(site_gemfile), binding)

